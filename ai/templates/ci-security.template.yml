name: Security Scanning

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '30 2 * * 0' # Weekly

jobs:
  sast:
    name: SAST (Placeholder)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run SAST Scanner
        run: |
          echo "Replace this with your language-specific SAST (e.g. CodeQL, Trivy, Bandit, etc.)"
          echo "This file satisfies the Day-1 SAST requirement in AI_RULES.md"

  dependabot:
    name: Dependabot Configuration Note
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check for dependabot.yml
        run: |
          if [ -f ".github/dependabot.yml" ]; then
            echo "dependabot.yml found."
          else
            echo "Remember to create .github/dependabot.yml to satisfy the Dependency Scanning rule."
          fi
